-- The TRUNCATE TABLE statement is used to delete the data inside a table, but not the table itself.
-- basically truncate table table ar vitorer data muce dite use kora hoy kintu table delete hoy na but drop table use korle pura table deleted hoye jay

truncate table person;