/*The ORDER BY keyword is used to sort the result-set in ascending or descending order.*/

/*it is sorting to the ascending order, when i use order by which is means is sort to the ascending order*/
Select * from Products
order by price;

/*The ORDER BY keyword sorts the records in ascending order by default. To sort the records in descending order, use the DESC keyword.*/

Select * from Products
order by price DESC;

-- numeric and alphabetically are work also similar that why we can use any of type when is needed

-- ORDER BY Several Columns
SELECT * FROM Customers
ORDER BY Country, CustomerName;

-- Using Both ASC and DESC

-- first country ascending form if i have two or more country name are same then customername have checked however there customername more priority than he is stay first

SELECT * FROM Customers
ORDER BY Country ASC, CustomerName DESC;